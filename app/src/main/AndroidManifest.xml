<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

<!--    Braodcast reciever -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="MadCcp"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MadCcp"
        tools:targetApi="31">

        <activity
            android:name=".ShapesLearning"
            android:exported="false" />
        <activity
            android:name=".MathsGame"
            android:exported="false" />
        <activity
            android:name=".ParentPortal"
            android:exported="false" />
        <activity
            android:name=".ProgressTracking"
            android:exported="false" />
        <activity
            android:name=".AlphabetLearning"
            android:exported="false" />
        <activity
            android:name=".Registration"
            android:exported="true">

            <!-- Remove intent-filter from here -->
        </activity>
        <activity
            android:name=".LoginAcitivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true" />
    </application>

</manifest>